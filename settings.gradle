pluginManagement {
    repositories {
        maven { url = "https://maven.fabricmc.net/" }
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "unified-legacy-settings"

gradle.ext.defaultMc = settings.providers
    .gradleProperty("defaultMc")
    .getOrElse("1.21.10")
